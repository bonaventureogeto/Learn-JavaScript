// if statements

let age = 17;

if (age >= 18 || age <=120) {
    console.log("You're eligible to VOTE");
} else if (age >= 65) {
    console.log("Retirement Age!")
} else {
    console.log("Enter the correct age.")
}

// write a JavaScript program that checks for someone's account and tells them
// they're broke if they have less than $10.
